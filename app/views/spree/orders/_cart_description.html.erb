<% product = variant.product
  if product.assembly? %>
	  <% product.parts.each do |v| %>
	  <%= product.count_of(v) %>&times; <%= link_to v.name, product_path(v.product) %> <%#= v.sku %><br />
  <% end %>
<% end %>
